spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5434/db_opsflow?currentSchema=msc_auth
    username: postgres
    password: 1234567890
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: msc_auth
        hbm2ddl.create_namespaces: true
  sql:
    init:
      mode: never
  data:
    redis:
      host: localhost
      port: 6379
  # CONFIGURACION SMTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: veronicacgarciap@gmail.com
    password: vdsn lqbz zizs ajuo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}

app:
  frontend-url: http://localhost:4200
  jwt:
    secret: =======================OpsFlowSecretKey2025BySeniorDev======================
    expiration: 3600000
    refreshExpiration: 604800000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  show-actuator: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics